defn gensymbol (s) {
    global counter
    name_with_id ≡ #strcons (s, subscripted_digit (counter))
    counter ⇐ counter + 1
    return name_with_id
}
defn print_stats (reg) {
    #print_stdout (7)
    #print_stdout (8)
    #print_stdout (9)
}
